<h1>Alle Registirerungen</h1>

<table>
	<tr>
		<th>Name</th>
		<th>Email</th>
		<th>Über</th>
		<th>Event</th>
		<th>Zeigen</th>
		<th>Ändern</th>
		<th>Löschen</th>

</tr>

<% @registrations.each do |registration| %>
<tr>
<td><%= registration.name  %></td>
<td><%= registration.email  %></td>
<td><%= registration.how_heard  %></td>
<td><%= registration.event_id  %></td>
<td>	<%= link_to 'show', event_registration_path(@event.id, registration.id )	%></td>
<td>	</td>
<td>	<%= link_to "Delete", events_path(@event
.id), class:"alert button "%></td>
</tr>
<%end%>
   </table> <br>
   <%= link_to "Back", event_path(@event.id), class:"success button "%>
    
