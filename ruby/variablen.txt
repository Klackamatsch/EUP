zuweisung:

variablen_Name = "dies ist ein -string"     <--- eine zuweisung erfolgt über den 
																										zuweisungsoperator '"'
																						<--- der variablentyp (string, integer, time, ....) erfolgt über die Eingabe
Zeichenkette (String) :
		str = "dies und 15 andere"
				Ein String wird erzeugt, in dem eine beliebige Anzahl von Zeichen ( Zahlen, Buchstaben) innerhalb von anführungszeichen stehen (sowohl doppelte als auch einfache ).

Zusammengesetzte Zeichenkette

		tag = "Freitag"
		str = "heute ist" +tag
																						<--- das + verbindet zwei strings
				das Ergebnis ist dann str %ENTER%  heute ist Ftreitag




Ganze Zahl (Integer, Fixnum)
		zahl = 15

Float (Kommazahl) 

		Komma_zahl = 13.44	


																wenn man eine methode auf eine Variable anwendet so ändert sich der wert der variable nicht dauerhaft, sondern nur für die eine methodenanwendung. schreibt man ein ausrufezeichen hinter die methode also string.reverse! so bleibt die änderung dauerhaft bestehen.


	.downcase.capitalize <----- downcase schriebt alles klein und capitalize schreibt die ersten buchstaben eines wortes immer groß

